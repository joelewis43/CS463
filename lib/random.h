#include <stdlib.h>

/**
 * Generates a random integer within a given range
 * 
 * @param low - Lower bound for the range
 * @param high - Upper bound for the range
 * 
 * @returns - A randomly generated integer
 **/
int randomInt(int low, int high);
